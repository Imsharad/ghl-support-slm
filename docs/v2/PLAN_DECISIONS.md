# V2 plan: gate decisions

Written 2026-09-07 14:40 IST by Fable 5.1 after reading `docs/plans/V2_PLAN.md` (Astra, 4c9373e seed, written 14:26 IST) and an independent Gemini 3.8 Flash critique of the plan against `docs/v2/SEED_PROMPT.md`. The plan is accepted with the amendments below. Where this file and the plan disagree, this file wins; the plan is kept unedited as the record of what was proposed.

| # | Plan said | Decision | Why |
|---|---|---|---|
| 1 | New wrapper `tools/v2.py` with 18 verbs as every gate's verify command (plan s5, s8) | Dropped. Verify commands are the existing scripts with explicit paths: `data/prepare.py --out data/v2`, `eval/check_challenge.py`, `eval/admission_scan.py`, `eval/run.py --challenge --seal`, `eval/blind.py`, `eval/score.py --sheet --key --scores --summary --failures`, `tools/check_run.py`, `tools/check_artifacts.py`, `tools/check_submission.py`. | `score.py` already takes every path flag (lines 42-48); Sol is adding `--challenge/--seal` to `run.py` and `blind.py`. A wrapper written tonight is untested code on the critical path. |
| 2 | Bootstrap 2,000 draws, disclosed against `configs/eval.yaml` 10,000 (plan s7) | Kept: 2,000, same as v1 executed (`eval/results/summary.jsonl` n_boot 2000). Disclosed in RESULTS. | Comparability with v1 outranks the config text; the mismatch is a v1 fact to report, not to fix mid-comparison. |
| 3 | Re-cluster from scratch (plan s5) | Accepted. Same MiniLM, threshold 0.86, average linkage, seed 42, grouping on raw instruction text. Group ids and split membership are not row-comparable with v1; `docs/v2/DATA_V2.md` says so. | The kept-id set moved by 4,318 rows; the pinned v1 grouping cannot be reused (Opus, V2-B1 report). |
| 4 | Admission rows count inside the 8,000 cap with no reserved slots (plan s4) | Changed. Rows flagged `SYNTH_ADMISSION` are exempt from the cap; the group-aware cap applies to corpus rows with `cap - n_admission`. Total capped train stays 8,000. Implemented in `cap_train_rows` so that v1 data (no synthetic rows) is byte-identical. | Sampling away about 60 percent of the intervention and then reporting v2's verdict would be uninterpretable. Same cap, same step count, one variable. |
| 5 | Authors write in a "fresh restricted session" (plan s4) | Row text is authored by a stateless model call (Gemini 3.8 Flash through `agy`, empty cwd, JSON schema) that sees only `data/intents.json`, the spec, and synthetic scenarios. Opus builds and runs the generator and the lint; Opus never writes row text. Grok-4.5 reviews rows under the same restriction. | Every channel agent has read RESULTS and FAILURES; the only truly fresh author available tonight is a stateless call. |
| 6 | Launch target 20:00, hard deadline 22:00, substrate lock 02:00 Tue (plan s6, s8) | Launch target 20:00, hard deadline 21:00, substrate lock 03:00 Tue. Colab is the headline if finished clean and downloaded by 03:00; else the Mac run; neither clean means v2 incomplete. | 22:00 plus the plan's own 5.75 h estimate lands after 02:00. |
| 7 | Leakage check of admissions against val/test (plan s4, s5) | Required as a named step after assembly: admissions versus `data/processed/v2/{val,test}.jsonl` and versus the three eval files, cosine under 0.80 and no shared 6-gram, results in `data/v2/leakage.json`. | The plan described the rule but scheduled no command for it. |
| 8 | Gate 2 adds "fresh ordinary passes at least base" (plan s7) | Kept, pre-registered here. It is the existing by-kind pass count from `eval/score.py`, not a new metric. A positive verdict that fails it is reported as positive and not promoted. | Over-refusal guard from the seed prompt; the rubric already fails an omitted request. |
| 9 | "your your" doubled-determiner defect in `replace_placeholders` (Opus, V2-B1 report: 1,771 v1 rows, 2,015 v2 rows) | Not fixed in v2. Recorded in `docs/v2/DATA_V2.md` and README as present in both runs. | Fixing it only in substitute mode adds a second variable; fixing it in both changes v1's evidence. |
| 10 | Admission quota 440 with the per-intent table (plan s4) | Kept as pre-registered design targets. Accepted rows may fall short; every intent must have at least one accepted row before assembly or the data gate stops. | Targets were set from corpus counts, not eval passes. |
| 11 | Row text authored by Gemini 3.8 Flash through `agy` (decision 5) | Changed 23:30 IST: row text is authored by `grok-4.5-build` through `grok -p` (stateless, empty cwd, JSON schema, `--verbatim`, no tools, no web). Same prompt, same schema, same lint, same restriction; `model` in every review line names the author. The one Flash-authored partial cell (1 row, 19:04 IST) is discarded, not mixed in. | Five Flash runs died silent between 18:56 and 19:09 IST: an `agy` call on this Mac fires a delayed kill that walks the parent chain (thread posts 19:06 to 19:10 IST, supervisor outside the session died too). One banked row in 2.5 hours. A stateless call from a different CLI keeps decision 5's restriction and removes the killer. |

Gate 1 (fresh sealed set) is unchanged: shark's approval on the rendered review page, then `eval/check_challenge.py --seal`. No admission row is generated before the seal exists.
