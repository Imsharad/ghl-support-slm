# Task D3: Checkpoint selection and Bitext test generation

- Worker: Opus
- Phase: D
- Due (IST): Mon 2026-09-07 10:30
- Estimate: 1.5 h
- Depends on: D2, B4, C1
- Repo: `/Users/sharad/Projects/agents-hq/gohighlevel-assignement-1`
- Read first: `docs/dag/CONTRACTS.md`, then the briefs of your dependencies under `docs/dag/tasks/`, then `docs/plans/E2E_PLAN_COMPRESSED_2026-09-05-2050IST.md` section 2 and the Astra plan sections 5 and 6 for the traps and evaluation rules.

## Owned paths (write only here)

- `artifacts/selection.json`
- `train/runs/main/`
- `eval/results/base-test-raw.jsonl`
- `eval/results/tuned-test-raw.jsonl`
- `eval/results/*-auto.json`
- `docs/SELECTION.md`

If you need a change outside these paths, describe it in your report; do not make it.

## Brief

Pick the checkpoint on dev only, then generate the Bitext test answers once.

Do:
- Unzip the run shark downloaded into `train/runs/main/`. Run `tools/check_run.py` and `train/plot_curves.py`.
- Score each checkpoint's dev raw file with `eval/auto_metrics.py` against `eval/dev.jsonl` acceptable answers where possible, and
  inspect 10 answers per checkpoint by rubric. Select by dev pass rate, then critical errors, then earlier checkpoint.
  Write `artifacts/selection.json` (`checkpoint, step, dev_pass_rate, critical_errors, reason, selected_at IST`) and `docs/SELECTION.md`.
- Generate Bitext test answers for base and the selected adapter (fp16, transformers backend). This needs the GPU: add a notebook cell
  or a script `eval/run_test_colab.py` that shark runs, or run on this Mac with MPS if the 270-row sample finishes under 90 minutes
  (measure 10 rows first). Sample 270 test rows (10 per intent across distinct groups, seed 42) and record the sample ids in
  `eval/results/test-sample.json`.
- Run `eval/auto_metrics.py --compare` and save both `-auto.json` files.

Verify: selection.json exists and cites dev numbers only; both test raw files have 270 rows; the auto-metrics comparison has an interval.

## Rules for every task

- Work in the repo above on `main`. Commit with `git add <your paths only>` and message `D3: <what>`; never `git add -A`; never amend or rebase; pull before commit if the index is locked, retry once.
- Run only under `uv run` in the repo venv (Python 3.11). System python is 3.14 and is off limits.
- No emojis anywhere. IST timestamps, labeled. No secrets in files (HF tokens stay in env).
- Do not start training, do not touch `eval/challenge.jsonl` after it is sealed, do not read blind keys.
- Report back in the GoHighLevel-prep thread (root `1981860808e7d870aa450555e16a336f0c9d20b710a0fee98e401b96ebf5c593`) with `@Fable 5.1`: what landed (paths), the verification command output, anything you could not do and why. One message when done or blocked; no acknowledgement messages.
